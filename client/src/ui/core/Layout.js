import React from 'react';
import {
  Container as MaterialContainer,
  Grid as MaterialGrid,
} from '@material-ui/core';

export const Container = (props) => <MaterialContainer {...props} />;
export const Grid = (props) => <MaterialGrid {...props} />;
